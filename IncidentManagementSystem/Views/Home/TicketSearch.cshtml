<div class="container-fluid dashboard">
    <div class="col-lg-12 header ">
        <div class="header-text">
            Ticket Details
        </div>
        <div class="col text">
            <span class="page input-icon">Ticket</span>

            <span class="detail">Ticket Details </span>
        </div>
    </div>

    @using (Html.BeginForm("TicketSearchInfo", "Home", FormMethod.Get, new { id = "searchForm", enctype = "multipart/form-data" }))
    {
        <div class="container-fluid search-box">
            <div class=" row s-box">


                <div class="col-lg-4 form-group s-dash">
                    @*@Html.TextBoxFor(model => model.SearchFor, new { @class = "form-control", placeholder = "Search" })*@
                    <input type="text" name="search" placeholder="Search..." class="form-control" } />
                </div>

                <div class="col-lg-4 s-dash">
                    <input type="submit" value="Search" class="btn btn-primary btn-width" />
                </div>

            </div>

        </div>

        <hr />
        <div id="partialTicketContainer">
            <h3>No Data </h3>
            @{
                Html.Action("TicketSearchInfo");
            }
        </div>
    }
</div>

@section scripts {
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.5.1.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#searchForm').submit(function (event) {
                event.preventDefault();
                $.ajax({
                    url: $(this).attr('action'),
                    type: $(this).attr('method'),
                    data: $(this).serialize(),
                    success: function (result) {
                        $('#partialTicketContainer').html(result);
                    },
                    error: function (xhr, status, error) {
                        alert('Error: ' + error);
                    }
                });
            });

            //$('#createUser').onClick(function () {

            //});
        });
    </script>
}
